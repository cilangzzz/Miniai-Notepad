# MiniAI记事本 - 共享组件功能实现方案

## 文档信息
- **项目名称：** KINETIC_NOTES（MiniAI记事本）
- **设计风格：** Neo-Brutalist
- **创建日期：** 2026-04-20

---

## 1. 设计系统核心规格

### 1.1 色彩令牌
```typescript
// TailwindCSS 配置扩展
const neoColors = {
  background: '#131313',      // Void Black - 主背景
  primary: '#FFD700',         // Kinetic Gold - 主强调
  secondary: '#007F7F',       // Teal Depth - 次强调
  surface: '#131313',         // Layer 0
  surfaceLowest: '#0e0e0e',   // Layer 1 凹陷
  surfaceHigh: '#2a2a2a',     // Layer 2 提升
  surfaceHighest: '#353535',  // 最高层级
  border: '#FFFFFF',          // Crisp White
  text: '#e2e2e2',            // Charcoal Text
  error: '#93000a',           // Error Crimson
  cyan: '#00f1ff',            // Cyan Accent
}
```

### 1.2 阴影系统
```typescript
// Neo-Brutalist 偏移阴影
const neoShadows = {
  black: '8px 8px 0px 0px #000000',    // 默认卡片
  white: '8px 8px 0px 0px #ffffff',    // 高对比
  gold: '8px 8px 0px 0px #FFD700',     // 强调级
  teal: '8px 8px 0px 0px #007F7F',     // 侧边栏
  hard: '6px 6px 0px 0px #353535',     // FAB
}
```

### 1.3 字体规格
```typescript
const neoFonts = {
  headline: ['Space Grotesk', 'sans-serif'],  // 标题
  body: ['Manrope', 'sans-serif'],            // 正文
  label: ['Space Grotesk', 'sans-serif'],     // 标签
}
```

---

## 2. TopAppBar 顶部导航栏

### 2.1 组件规格
```vue
<!-- components/common/TopAppBar.vue -->
<script setup lang="ts">
interface Props {
  title?: string           // 品牌标题，默认 "KINETIC_NOTES"
  showMenu?: boolean       // 显示菜单按钮
  showSearch?: boolean     // 显示搜索按钮
  showAvatar?: boolean     // 显示用户头像
}

const props = withDefaults(defineProps<Props>(), {
  title: 'KINETIC_NOTES',
  showMenu: true,
  showSearch: true,
  showAvatar: true,
})

const emit = defineEmits<{
  menuClick: []
  searchClick: []
  avatarClick: []
}>()
</script>

<template>
  <header class="
    fixed top-0 z-50 w-full
    bg-background border-b-4 border-white
    flex justify-between items-center
    px-6 py-4
  ">
    <!-- 左侧：菜单 + Logo -->
    <div class="flex items-center gap-4">
      <button 
        v-if="showMenu"
        @click="emit('menuClick')"
        class="text-white hover:skew-x-1 transition-transform duration-150"
      >
        <span class="material-symbols-outlined text-3xl">menu</span>
      </button>
      
      <h1 class="
        text-2xl font-black italic
        text-primary -skew-x-2
        font-headline uppercase tracking-tighter
      ">
        {{ title }}
      </h1>
    </div>
    
    <!-- 右侧：搜索 + Avatar -->
    <div class="flex items-center gap-4">
      <button 
        v-if="showSearch"
        @click="emit('searchClick')"
        class="text-white hover:skew-x-1 transition-transform duration-150"
      >
        <span class="material-symbols-outlined text-3xl">search</span>
      </button>
      
      <button 
        v-if="showAvatar"
        @click="emit('avatarClick')"
        class="
          w-10 h-10
          bg-secondary border-2 border-white
          flex items-center justify-center overflow-hidden
        "
      >
        <img src="/avatar.png" alt="Avatar" class="w-full h-full object-cover" />
      </button>
    </div>
  </header>
</template>
```

### 2.2 样式规格表
| 属性 | Tailwind类 | 值 |
|------|-----------|-----|
| 定位 | `fixed top-0 z-50` | 固定顶部 |
| 背景 | `bg-background` | #131313 |
| 边框 | `border-b-4 border-white` | 4px底部白边框 |
| Logo倾斜 | `-skew-x-2` | -2度 |
| Logo颜色 | `text-primary` | #FFD700 |
| Logo字体 | `font-headline font-black italic` | Space Grotesk Black |
| Hover过渡 | `hover:skew-x-1 transition-transform duration-150` | 150ms倾斜 |

---

## 3. BottomNavBar 底部导航栏（Mobile）

### 3.1 组件规格
```vue
<!-- components/common/BottomNavBar.vue -->
<script setup lang="ts">
interface NavItem {
  id: string
  label: string
  icon: string
  route: string
}

interface Props {
  items?: NavItem[]
  activeId?: string
}

const defaultItems: NavItem[] = [
  { id: 'notes', label: 'Notes', icon: 'sticky_note_2', route: '/notes' },
  { id: 'tags', label: 'Tags', icon: 'label', route: '/categories' },
  { id: 'archive', label: 'Archive', icon: 'archive', route: '/archive' },
  { id: 'finance', label: 'Finance', icon: 'account_balance_wallet', route: '/finance' },
  { id: 'settings', label: 'Settings', icon: 'settings', route: '/settings' },
]

const props = withDefaults(defineProps<Props>(), {
  items: defaultItems,
  activeId: 'notes',
})

const emit = defineEmits<{
  navigate: [route: string]
}>()
</script>

<template>
  <nav class="
    md:hidden fixed bottom-0 left-0 z-50 w-full
    bg-background border-t-4 border-white
    flex justify-around items-center
    px-4 pb-6 pt-2
  ">
    <button
      v-for="item in items"
      :key="item.id"
      @click="emit('navigate', item.route)"
      :class="[
        'flex flex-col items-center justify-center px-4 py-1 transition-all duration-200',
        item.id === activeId
          ? 'bg-primary text-black border-2 border-white -translate-y-2 -translate-x-1 shadow-[4px_4px_0px_0px_rgba(255,255,255,1)]'
          : 'text-white/70 hover:bg-secondary'
      ]"
    >
      <span class="material-symbols-outlined">{{ item.icon }}</span>
      <span class="font-headline font-bold text-[10px] uppercase">{{ item.label }}</span>
    </button>
  </nav>
</template>
```

### 3.2 样式规格表
| 属性 | Tailwind类 | 值 |
|------|-----------|-----|
| 定位 | `fixed bottom-0` | 固定底部 |
| 显示 | `md:hidden` | 仅Mobile显示 |
| 背景 | `bg-background` | #131313 |
| 边框 | `border-t-4 border-white` | 4px顶部白边框 |
| 激活态填充 | `bg-primary` | #FFD700 |
| 激活态位移 | `-translate-y-2 -translate-x-1` | 上移左移 |
| 激活态阴影 | `shadow-[4px_4px_0px_0px_rgba(255,255,255,1)]` | 白色Neo阴影 |
| 标签字体 | `font-headline font-bold text-[10px] uppercase` | Space Grotesk Bold 10px |

---

## 4. NavigationDrawer 侧边栏导航（Desktop）

### 4.1 组件规格
```vue
<!-- components/common/NavigationDrawer.vue -->
<script setup lang="ts">
interface Category {
  id: string
  name: string
  icon: string
  color?: string
}

interface Props {
  categories?: Category[]
  activeId?: string
  storageUsed?: number  // 0-100
}

const defaultCategories: Category[] = [
  { id: 'work', name: 'Work', icon: 'work', color: '#FFD700' },
  { id: 'personal', name: 'Personal', icon: 'person' },
  { id: 'ideas', name: 'Ideas', icon: 'lightbulb' },
  { id: 'tasks', name: 'Tasks', icon: 'check_circle' },
]

const props = withDefaults(defineProps<Props>(), {
  categories: defaultCategories,
  activeId: 'work',
  storageUsed: 67,
})

const emit = defineEmits<{
  select: [categoryId: string]
}>()
</script>

<template>
  <aside class="
    hidden md:flex fixed left-0 top-[72px] h-full w-72
    flex-col gap-6 p-8
    bg-background border-r-4 border-white
    shadow-[8px_0px_0px_0px_rgba(0,127,127,1)]
    z-40
  ">
    <!-- 分类导航 -->
    <div class="mt-4">
      <h2 class="
        font-headline font-bold text-secondary text-xs
        uppercase tracking-widest mb-6
      ">
        CATEGORIES
      </h2>
      
      <nav class="flex flex-col gap-4">
        <a
          v-for="cat in categories"
          :key="cat.id"
          @click="emit('select', cat.id)"
          :class="[
            'px-4 py-3 flex items-center gap-3 font-headline transition-transform duration-200',
            cat.id === activeId
              ? 'bg-secondary text-white font-black border-2 border-white translate-x-2'
              : 'text-white/80 hover:translate-x-1 hover:text-primary'
          ]"
        >
          <span class="material-symbols-outlined">{{ cat.icon }}</span>
          {{ cat.name }}
        </a>
      </nav>
    </div>
    
    <!-- 存储容量指示器 -->
    <div class="mt-auto pb-24">
      <div class="
        p-4 bg-surfaceHigh border-2 border-white
        shadow-[6px_6px_0px_0px_rgba(255,255,255,1)]
      ">
        <p class="text-xs font-bold uppercase mb-2">Storage</p>
        <div class="h-4 w-full bg-black border-2 border-white">
          <div 
            class="h-full bg-primary transition-all duration-300"
            :style="{ width: `${storageUsed}%` }"
          />
        </div>
        <p class="text-xs text-white/60 mt-2">{{ storageUsed }}% used</p>
      </div>
    </div>
  </aside>
</template>
```

### 4.2 样式规格表
| 属性 | Tailwind类 | 值 |
|------|-----------|-----|
| 定位 | `fixed left-0 top-[72px]` | 左侧固定，避开顶部栏 |
| 显示 | `hidden md:flex` | 仅Desktop显示 |
| 宽度 | `w-72` | 288px |
| 边框 | `border-r-4 border-white` | 4px右侧白边框 |
| 阴影 | `shadow-[8px_0px_0px_0px_rgba(0,127,127,1)]` | 青色侧阴影 |
| 激活分类 | `bg-secondary translate-x-2 border-2` | 青色填充+偏移 |

---

## 5. NeoFAB 浮动操作按钮

### 5.1 组件规格
```vue
<!-- components/base/NeoFAB.vue -->
<script setup lang="ts">
interface Props {
  icon?: string
  color?: 'gold' | 'white' | 'teal'
  size?: 'sm' | 'md' | 'lg'
}

const props = withDefaults(defineProps<Props>(), {
  icon: 'add',
  color: 'gold',
  size: 'lg',
})

const colorClasses = {
  gold: 'bg-primary text-black shadow-[8px_8px_0px_0px_rgba(0,0,0,1)]',
  white: 'bg-white text-black shadow-[8px_8px_0px_0px_rgba(0,0,0,1)]',
  teal: 'bg-secondary text-white shadow-[8px_8px_0px_0px_rgba(0,0,0,1)]',
}

const sizeClasses = {
  sm: 'w-12 h-12',
  md: 'w-16 h-16',
  lg: 'w-20 h-20',
}

const emit = defineEmits<{
  click: []
}>()
</script>

<template>
  <button
    @click="emit('click')"
    :class="[
      'fixed bottom-32 right-8 z-[60]',
      'border-4 border-white rounded-none',
      'flex items-center justify-center',
      'hover:scale-105 transition-all duration-200',
      'active:translate-y-2 active:shadow-none',
      sizeClasses[size],
      colorClasses[color],
    ]"
  >
    <span class="material-symbols-outlined text-5xl font-bold">{{ icon }}</span>
  </button>
</template>
```

### 5.2 样式规格表
| 属性 | Tailwind类 | 值 |
|------|-----------|-----|
| 尺寸 | `w-20 h-20` | 80px (lg) |
| 填充 | `bg-primary` | #FFD700 |
| 边框 | `border-4 border-white rounded-none` | 4px白边框、无圆角 |
| 阴影 | `shadow-[8px_8px_0px_0px_rgba(0,0,0,1)]` | 黑色Neo阴影 |
| Hover | `hover:scale-105` | 放大5% |
| Active | `active:translate-y-2 active:shadow-none` | 下压+阴影消失 |

---

## 6. NeoCard 卡片组件

### 6.1 组件规格
```vue
<!-- components/base/NeoCard.vue -->
<script setup lang="ts">
interface Props {
  variant?: 'primary' | 'secondary' | 'accent' | 'dark'
  padding?: 'sm' | 'md' | 'lg'
  hoverable?: boolean
}

const props = withDefaults(defineProps<Props>(), {
  variant: 'secondary',
  padding: 'md',
  hoverable: true,
})

const variantClasses = {
  primary: 'bg-secondary',       // #007F7F
  secondary: 'bg-surfaceHigh',   // #2a2a2a
  accent: 'bg-primary',          // #FFD700
  dark: 'bg-surfaceLowest',      // #0e0e0e
}

const paddingClasses = {
  sm: 'p-4',
  md: 'p-6',
  lg: 'p-8',
}
</script>

<template>
  <article
    :class="[
      'relative border-4 border-white rounded-none',
      'shadow-[8px_8px_0px_0px_rgba(0,0,0,1)]',
      paddingClasses[padding],
      variantClasses[variant],
      hoverable && 'hover:-translate-y-1 hover:-translate-x-1 transition-all duration-250 cursor-pointer',
    ]"
  >
    <slot />
  </article>
</template>
```

---

## 7. NeoTag 标签组件

### 7.1 组件规格
```vue
<!-- components/base/NeoTag.vue -->
<script setup lang="ts">
interface Props {
  label: string
  color?: 'gold' | 'teal' | 'white' | 'gray'
  rotation?: number  // -3 到 3
  selected?: boolean
  removable?: boolean
}

const props = withDefaults(defineProps<Props>(), {
  color: 'teal',
  rotation: 0,
  selected: false,
  removable: false,
})

const colorClasses = {
  gold: 'bg-primary text-black border-white',
  teal: 'bg-secondary text-white border-white',
  white: 'bg-white text-black border-black',
  gray: 'bg-surfaceHighest text-white border-white',
}

const emit = defineEmits<{
  click: []
  remove: []
}>()
</script>

<template>
  <span
    @click="emit('click')"
    :class="[
      'inline-flex items-center gap-1',
      'px-2 py-1 border-2 rounded-none',
      'font-headline font-bold text-[10px] uppercase',
      'transition-transform duration-300',
      colorClasses[color],
      selected && 'ring-2 ring-primary ring-offset-2 ring-offset-background',
    ]"
    :style="{ transform: `rotate(${rotation}deg)` }"
  >
    {{ label }}
    
    <button
      v-if="removable"
      @click.stop="emit('remove')"
      class="hover:text-primary ml-1"
    >
      <span class="material-symbols-outlined text-xs">close</span>
    </button>
  </span>
</template>
```

### 7.2 Hover效果
```css
/* 标签Hover归位效果 */
.neo-tag:hover {
  transform: rotate(0deg) !important;
}
```

---

## 8. NeoInput 输入框组件

### 8.1 组件规格
```vue
<!-- components/base/NeoInput.vue -->
<script setup lang="ts">
interface Props {
  modelValue: string
  placeholder?: string
  size?: 'sm' | 'md' | 'lg'
  disabled?: boolean
}

const props = withDefaults(defineProps<Props>(), {
  placeholder: '',
  size: 'md',
  disabled: false,
})

const sizeClasses = {
  sm: 'p-3 text-sm',
  md: 'p-4 text-base',
  lg: 'p-6 text-lg',
}

const emit = defineEmits<{
  'update:modelValue': [value: string]
}>()
</script>

<template>
  <input
    :value="modelValue"
    @input="emit('update:modelValue', $event.target.value)"
    :placeholder="placeholder"
    :disabled="disabled"
    :class="[
      'w-full rounded-none',
      'bg-surfaceLowest border-4 border-white',
      'text-text placeholder:text-surfaceHighest',
      'font-body',
      'focus:border-primary focus:outline-none',
      'focus:shadow-[2px_2px_0px_0px_rgba(255,255,255,0.5)]',
      'transition-all duration-200',
      sizeClasses[size],
      disabled && 'opacity-50 cursor-not-allowed',
    ]"
  />
</template>
```

---

## 9. NeoButton 按钮组件

### 9.1 组件规格
```vue
<!-- components/base/NeoButton.vue -->
<script setup lang="ts">
interface Props {
  variant?: 'primary' | 'secondary' | 'tertiary'
  size?: 'sm' | 'md' | 'lg'
  icon?: string
}

const props = withDefaults(defineProps<Props>(), {
  variant: 'primary',
  size: 'md',
})

const variantClasses = {
  primary: 'bg-primary text-black border-4 border-white shadow-[8px_8px_0px_0px_rgba(0,0,0,1)]',
  secondary: 'bg-transparent text-white border-4 border-white',
  tertiary: 'bg-transparent text-white border-none underline underline-offset-4 decoration-white',
}

const sizeClasses = {
  sm: 'px-4 py-1 text-xs',
  md: 'px-6 py-2 text-sm',
  lg: 'px-8 py-4 text-base',
}

const emit = defineEmits<{
  click: []
}>()
</script>

<template>
  <button
    @click="emit('click')"
    :class="[
      'inline-flex items-center gap-2',
      'rounded-none font-headline font-bold uppercase',
      'transition-all duration-150',
      'hover:-translate-y-1 hover:-translate-x-1',
      'active:translate-y-2 active:shadow-none',
      variantClasses[variant],
      sizeClasses[size],
    ]"
  >
    <span v-if="icon" class="material-symbols-outlined">{{ icon }}</span>
    <slot />
  </button>
</template>
```

---

## 10. BentoGrid 网格布局组件

### 10.1 组件规格
```vue
<!-- components/common/BentoGrid.vue -->
<script setup lang="ts">
interface Props {
  columns?: 1 | 2 | 3
  gap?: number
}

const props = withDefaults(defineProps<Props>(), {
  columns: 3,
  gap: 8,
})

// 响应式列数
const columnClasses = computed(() => {
  switch (props.columns) {
    case 1: return 'grid-cols-1'
    case 2: return 'grid-cols-1 md:grid-cols-2'
    case 3: return 'grid-cols-1 md:grid-cols-2 lg:grid-cols-3'
    default: return 'grid-cols-1 md:grid-cols-2 lg:grid-cols-3'
  }
})
</script>

<template>
  <div
    :class="[
      'grid',
      columnClasses.value,
      `gap-${props.gap}`,
    ]"
  >
    <slot />
  </div>
</template>
```

---

## 11. Toast 提示组件

### 11.1 组件规格
```vue
<!-- components/common/NeoToast.vue -->
<script setup lang="ts">
interface Props {
  message: string
  type?: 'success' | 'error' | 'warning'
  duration?: number
}

const props = withDefaults(defineProps<Props>(), {
  type: 'success',
  duration: 1000,
})

const typeClasses = {
  success: 'bg-primary text-black border-white',
  error: 'bg-error text-white border-white',
  warning: 'bg-secondary text-white border-white',
}

const visible = ref(true)

onMounted(() => {
  setTimeout(() => {
    visible.value = false
  }, props.duration)
})
</script>

<template>
  <Transition name="toast">
    <div
      v-if="visible"
      :class="[
        'fixed top-20 left-1/2 -translate-x-1/2 z-[100]',
        'px-6 py-3 border-4 rounded-none',
        'font-headline font-bold uppercase',
        'shadow-[4px_4px_0px_0px_rgba(0,0,0,1)]',
        typeClasses[type],
      ]"
    >
      {{ message }}
    </div>
  </Transition>
</template>

<style>
.toast-enter-active,
.toast-leave-active {
  transition: all 200ms ease;
}
.toast-enter-from,
.toast-leave-to {
  opacity: 0;
  transform: translate(-50%, -20px);
}
</style>
```

---

## 12. 响应式布局适配

### 12.1 断点定义
```typescript
// composables/useResponsive.ts
export function useResponsive() {
  const breakpoints = {
    mobile: 768,    // < 768px
    tablet: 1024,  // 768-1024px
    desktop: 1280, // > 1024px
  }
  
  const width = ref(window.innerWidth)
  
  const isMobile = computed(() => width.value < breakpoints.mobile)
  const isTablet = computed(() => 
    width.value >= breakpoints.mobile && width.value < breakpoints.tablet
  )
  const isDesktop = computed(() => width.value >= breakpoints.tablet)
  
  const columns = computed(() => {
    if (isMobile.value) return 1
    if (isTablet.value) return 2
    return 3
  })
  
  return { isMobile, isTablet, isDesktop, columns }
}
```

---

## 附录：组件命名规范

| 层级 | 前缀 | 示例 |
|------|------|------|
| Base | `Neo` | NeoButton, NeoCard, NeoTag |
| Common | 功能名 | TopAppBar, BottomNavBar, BentoGrid |
| Business | 模块前缀 | NoteCard, FinanceCard, NewsCard |
| Page | 页面名 | MyNotes, EditNote, Categories |